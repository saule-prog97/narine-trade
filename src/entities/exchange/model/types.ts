export interface Exchange {
  id: string
  name: string
  /** Двухбуквенная монограмма: у половины бирж имена начинаются на «B». */
  mark: string
  /** Фирменный цвет биржи — подсвечивает монограмму. */
  accent: string
  /** Реферальная ссылка со скидкой на комиссии. */
  url: string
  /** Короткая подпись под названием биржи. */
  note: string
}
